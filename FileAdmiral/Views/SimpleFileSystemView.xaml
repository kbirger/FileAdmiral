<UserControl x:Class="FileAdmiral.Views.SimpleFileSystemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ListView ItemsSource="{Binding Items}" x:Name="lvItems">
        <ListView.InputBindings>
            <KeyBinding Gesture="Enter"  Command="{Binding ChangeDirectoryCommand}" CommandParameter="{Binding ElementName=lvItems, Path=SelectedItem}"/>
            <KeyBinding Gesture="Backspace" Command="{Binding GoUpCommand}" />
        </ListView.InputBindings>
    </ListView>
</UserControl>
